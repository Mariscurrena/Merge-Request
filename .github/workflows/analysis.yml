name: Fortify Static Application Security Testing

on:
    workflow_call:

jobs:
    analysis:
        name: Fortify Static Application Security Testing
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v2 